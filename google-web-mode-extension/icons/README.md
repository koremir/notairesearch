# Icons

This folder is reserved for Chrome extension icons.

For production, add PNG icons such as:

- `icon-16.png`
- `icon-32.png`
- `icon-48.png`
- `icon-128.png`

The extension works without custom icons; Chrome will show the default extension icon until real assets are added.
